# 로드맵

이 문서는 구현하지 않은 확장 방향을 채권운용본부 실무 관점에서 정리한
것이다. 각 항목은 "운용역에게 주는 가치 / 필요한 데이터 / 구현 난이도 /
에이전트 역할"로 정리했다.

---

## 단기 (1~2개월)

### 1. 금투협 등급·섹터별 민평 스프레드 — ✅ 부분 구현됨 (Phase E, 2026-09-25)

- **구현 현황**: `bond_agent/tools/credit.py: get_credit_curve()` +
  `bond_agent/analytics/credit.py: calc_credit_context()`로 등급별/섹터별
  민평 스프레드의 레벨·1일/1주/1개월 변동·percentile(현재 60영업일 기준,
  아래 남은 과제 참고)·regime(타이트/중립/와이드)·등급간/섹터간 확대·축소
  방향까지 코드로 계산해서 크레딧 섹션에 반영 중.
- **실제로 쓴 데이터 소스**: 애초 예상했던 KOFIA 채권정보센터
  (`kofiabond.or.kr`) 자체 사이트는 죽은 legacy frameset이라 못 썼다.
  대신 KAP(한국자산평가, `koreaap.com`)의 공개 AJAX 엔드포인트를 JS
  소스 직접 조회로 찾아서 사용 - ECOS 시장금리와 교차검증해서 신뢰성
  확인함 (예: 국고채 3Y KAP 3.997% vs ECOS 4.006%, 2026-09-23 기준).
- **남은 과제**: percentile의 lookback이 스펙(1년=252영업일)이 아니라
  60영업일(약 3개월)이다 - 252영업일 순차 조회가 브리핑 1회당 4분+
  걸려서 임시로 줄인 것. 매일 새 하루치만 캐시에 추가하는 증분 백필
  스크립트를 만들면 순차 재조회 없이도 실제 1년치를 유지할 수 있을
  것 - 다음 사람이 시도해볼 만한 항목.
- **발행사 개별 확정 가산금리를 등급 평균과 자동 비교하는 코멘트**는
  아직 없음 (`get_bond_demand_forecasts`와 `get_credit_snapshot` 결과를
  LLM이 서술로만 연결하고 있고, 코드로 "동일 등급 평균보다 Nbp
  낮게/높게"를 계산해서 붙이는 것은 여전히 남은 작업).

### 2. 국채선물 basis 및 외국인 선물 수급 — ⚠️ 조사 완료, 실제로 막혀 있음을 확인 (Phase E, 2026-09-25)

- **운용역에게 주는 가치**: 현물-선물 basis와 외국인 선물 순매수/순매도는
  현물 금리보다 하루 앞서 시장 방향성을 시사하는 경우가 많다. 지금
  브리핑은 현물 금리만 다루므로 이 정보가 없으면 놓치는 신호가 있다.
- **필요한 데이터**: KRX 국채선물 종가/basis, 외국인 순매수.
- **실제로 확인된 것 (Phase E 이전엔 추측이었는데 이제 실측함)**: KRX
  `data.krx.co.kr`는 예전엔 무료 공개 JSON API(`comm/bldAttendant/
  getJsonData.cmd`)였는데, 최근 "Data Marketplace"로 개편되면서 이
  API 자체가 로그인 세션을 요구하도록 바뀐 것을 Python으로 직접 요청해서
  확인했다 (`pykrx` 라이브러리가 쓰던 예전 bld 파라미터로 요청해도
  익명 세션 쿠키로는 `400 LOGOUT` 응답). 즉 "무료 지연 데이터"라는 옛
  가정은 더 이상 유효하지 않다 - **현재는 로그인 없이 이 데이터를 가져올
  공개 경로가 없다.** 2차 후보였던 금투협 FreeSIS
  (`freesis.kofia.or.kr`)도 컴파일된 엔터프라이즈 JS 프레임워크(Nexacro/
  Xplatform 계열 추정)라 역공학이 사실상 불가능함을 확인했다.
- **구현 난이도**: 이제 "중간"이 아니라 **사실상 유료 단말(인포맥스/
  에프앤가이드) 또는 KRX와의 정식 데이터 라이선스 계약이 필수**로
  바뀐 것으로 봐야 한다 - 무료 스크래핑으로 우회할 방법을 Phase E에서
  상당한 시간을 들여 찾아봤지만 못 찾음 (`WORK_LOG.md` Phase 2-2/2-3
  참고). 다음 시도자는 이 경로부터 다시 찾기보다, 유료 단말 연동
  가능 여부부터 확인하는 게 시간을 아낄 수 있다.
- **에이전트 역할**: `get_market_snapshot`과 병렬로 `get_futures_snapshot`
  같은 툴을 추가하고, basis가 평소 범위를 벗어나면(z-score) 섹션에
  같이 언급하도록 시스템 프롬프트를 확장 (유료 데이터 소스 확보 후
  유효한 설계).

### 3. 국고채 입찰 결과 트래킹 — ✅ 부분 구현됨 (월별 발행 실적만, Phase E)

- **운용역에게 주는 가치**: 입찰 낙찰금리와 응찰률(bid-to-cover)은 그
  구간 수급을 가장 직접적으로 보여준다. 입찰이 있는 날은 브리핑에서
  꼭 다뤄야 하는 이벤트인데, 개별 입찰 상세는 여전히 빠져 있다 (아래
  "실제로 구현된 것" 참고).
- **실제로 구현된 것**: `bond_agent/tools/ktb_supply.py: get_issuance_plan(month)`가
  ECOS 191Y001(주요 국공채 발행액/잔액, 국고채권 항목)에서 월별 발행
  실적과 전년 동월 대비 증감(YoY %)을 가져와 국채수급 섹션에 반영 중.
- **왜 "낙찰금리/응찰률"까지는 못 갔나**: 애초 예상했던 "기획재정부/한국은행
  보도자료 RSS로 접근 가능할 가능성이 높다"는 추측이 실제로는 맞지
  않았다. `ktb.moef.go.kr/bidResult.do`는 페이지가 범용 JS로 데이터를
  주입하는 방식이라(KAP 때처럼 raw JS를 읽어 엔드포인트를 특정하는
  방법이 안 통함) 1.5시간 타임박스 안에 실제 데이터 엔드포인트를
  찾지 못했다. data.go.kr의 관련 데이터셋(`fileData` 유형)은 별도
  API 키 신규 등록이 필요해서 보류.
- **남은 과제(구현 난이도: 낮음~중간, 변화 없음)**: `ktb.moef.go.kr`의
  실제 데이터 엔드포인트를 더 깊이 파거나(개발자 도구로 실제 브라우저
  네트워크 탭을 직접 봐야 할 수도 있음 - 이번엔 raw HTML/JS만 봤음),
  data.go.kr API 키를 새로 발급받아 `fileData` 데이터셋에 접근하는
  경로를 시도해볼 만하다.
- **에이전트 역할**: 이미 `get_ktb_supply` 툴로 반영됨. 개별 입찰 상세를
  확보하면 `get_market_snapshot`을 호출하기 전에 오늘이 국고채 입찰일인지
  확인하는 툴을 추가하고, 입찰일이면 국채수급 섹션에 낙찰금리·응찰률을
  자동으로 포함하도록 시스템 프롬프트에 조건 추가.

### 4. 금통위 의사록·FOMC 성명 톤 변화 분석

- **운용역에게 주는 가치**: 금리 결정 자체보다 "지난번 대비 톤이
  얼마나 매파적/비둘기파적으로 바뀌었는지"가 시장에 더 큰 영향을 준다.
  지금은 뉴스 검색으로 헤드라인만 보여주고 톤 변화를 정량화하지 않는다.
- **필요한 데이터**: 한국은행 금통위 의사록 전문(한은 RSS로 이미
  접근 경로 있음), Fed FOMC statement 전문(연준 웹사이트).
- **구현 난이도**: 중간. 원문 수집은 쉽지만, "톤 변화"를 코드로
  정량화하려면 이전 회차와의 diff + 키워드 빈도 변화 정도가 현실적
  (LLM 감성분석은 숫자를 만들어내는 것에 해당해서 이 프로젝트의
  설계 원칙과 충돌 - 엄격한 키워드/구문 매칭 기반으로 제한해야 함).
- **에이전트 역할**: 코드가 "이전 회차 대비 등장/소멸한 키워드"를 계산해
  전달하고, LLM은 그 diff를 해석해서 서술만 한다 (이 프로젝트 설계
  원칙 그대로 확장).

---

## 중기 (3~6개월)

### 5. 포트폴리오 연동 (듀레이션, KRD, 금리 시나리오별 손익)

- **운용역에게 주는 가치**: 지금 브리핑은 "시장이 어떻게 움직였는지"만
  말하고 "그래서 우리 포트폴리오에 얼마나 영향이 있는지"는 말하지 않는다.
  실제 듀레이션/KRD(Key Rate Duration)를 알면 "오늘 금리 변동으로 우리
  포트폴리오는 대략 N억 손익"까지 계산해서 알려줄 수 있다 - 이게 있으면
  브리핑이 "시장 리포트"에서 "내 포트폴리오 리포트"로 바뀐다.
- **필요한 데이터**: 사내 포트폴리오 시스템(OMS/PMS)의 포지션 데이터
  (종목별 듀레이션, KRD, 보유금액). 사내 시스템 API 또는 정기 파일
  export 연동이 필요.
- **구현 난이도**: 높음. 데이터 자체는 사내에 있어서 기술적으로는
  파이프라인 연동 문제지만, 사내 시스템 접근 권한/보안 검토가 선행
  조건이라 실제 소요 시간은 기술 난이도보다 조직적 절차에 더 좌우됨.
- **에이전트 역할**: `calc_daily_changes`가 계산한 만기별 bp 변동에
  포트폴리오 KRD를 곱해서 손익을 코드로 계산 (여전히 숫자는 코드가
  계산, LLM은 서술만 - 설계 원칙 그대로 확장됨). 새 섹션 "포트폴리오
  영향"을 브리핑에 추가.

### 6. ACM 방식 term premium 분해 모니터링

- **운용역에게 주는 가치**: 장기금리 상승이 "기대 정책금리 경로 변화"
  때문인지 "term premium(기간 프리미엄) 확대" 때문인지를 구분하면 대응이
  달라진다 (전자는 정책 전망 재점검, 후자는 수급/불확실성 프리미엄
  문제). 지금 브리핑은 이 구분 없이 레벨/변동만 보여준다.
- **필요한 데이터**: ACM(Adrian-Crump-Moench) term premium은 뉴욕
  연은이 매일 공개하는 추정치가 있어 직접 계산하지 않고 가져다 쓸 수
  있다 (한국은 이런 공식 추정치가 없어서, 미국 term premium 모니터링
  + 한국은 자체 근사 모델이 필요해 난이도가 갈림).
- **구현 난이도**: 중간(미국) ~ 높음(한국 자체 모델). 미국 쪽은
  뉴욕연은 공개 데이터를 그대로 받아오면 되지만, 한국 term premium을
  직접 분해하려면 통계 모델링이 필요.
- **에이전트 역할**: 새 데이터 툴로 term premium 시계열을 받아와서,
  `flag_anomalies`와 같은 방식으로 term premium 자체의 z-score를
  계산해 이상치 판단에 포함.

### 7. 커브 RV 시그널(스프레드·버터플라이 z-score)과 간단한 백테스트

- **운용역에게 주는 가치**: 지금 `flag_anomalies`는 레벨/스프레드의
  z-score만 본다. 버터플라이(예: 2×5년-3년-10년 같은 조합) z-score와
  그 시그널의 과거 평균회귀 성공률(간단 백테스트)까지 있으면, "이
  스프레드가 지금 극단적인데, 과거에 이 정도 벌어졌을 때 보통 며칠 안에
  좁혀졌는지"를 같이 보여줄 수 있어 RV(relative value) 트레이드 판단에
  직접 도움이 된다.
- **필요한 데이터**: 이미 확보한 국고채 커브 데이터로 충분 (추가 데이터
  소스 불필요) - 계산 로직만 확장하면 됨.
- **구현 난이도**: 낮음~중간. `curve.py`에 버터플라이 조합 계산과
  "z-score N 이상일 때 M영업일 후 평균회귀 비율" 같은 간단한 이벤트
  기반 백테스트 함수를 추가하는 정도.
- **에이전트 역할**: 백테스트 결과(과거 성공률)까지도 코드가 계산해서
  숫자로 제공하고, LLM은 "과거 이력상 평균회귀 확률이 X%"라고 그대로
  인용만 한다 (여전히 숫자는 코드, 설명은 LLM).

---

## 장기 (6개월 이상)

### 8. 역할 분리 멀티 에이전트 (매크로 리서치 / 크레딧 / 리스크 체크)

- **운용역에게 주는 가치**: 지금은 하나의 에이전트가 매크로 금리, 크레딧
  발행시장, 검증까지 다 처리한다. 역할을 나누면 각 서브에이전트가 더
  좁고 깊은 툴 세트와 프롬프트에 집중할 수 있어, 크레딧 섹션이 매크로
  섹션 때문에 얕아지는 것을 막고, 각 영역을 독립적으로 개선/교체할 수
  있다.
- **필요한 데이터**: 추가 데이터는 불필요 - 기존 툴들을 서브에이전트별로
  재배치하는 아키텍처 변경.
- **구현 난이도**: 높음. 서브에이전트 간 통신/오케스트레이션, 각
  서브에이전트의 결과를 최종 리포트로 합치는 조율 로직, 실패 시 부분
  결과 처리 등 고려할 게 많다.
- **에이전트 역할**: "매크로 리서치 에이전트"(국고채/미국 금리),
  "크레딧 에이전트"(수요예측/발행시장), "리스크 체크 에이전트"(verify.py
  역할을 능동적 역할로 확장 - 단순 사후 대조가 아니라 각 서브에이전트
  결과를 받아서 교차검증)로 분리하고, 오케스트레이터가 최종 리포트로 합침.

### 9. 리포트 품질 평가 체계 (숫자 정확도, 원인 귀속 정확도, 운용역 피드백 루프)

- **운용역에게 주는 가치**: 지금은 verify.py로 숫자 불일치만 기계적으로
  잡는다. 실제로 운용역이 매일 리포트를 읽고 "이 원인 분석이 맞았는지",
  "이 정도 디테일이 유용했는지"를 남길 수 있는 피드백 루프가 있으면,
  시스템 프롬프트나 툴 선택 기준을 데이터 기반으로 개선할 수 있다.
- **필요한 데이터**: 운용역의 일일 피드백(간단한 좋아요/싫어요 + 코멘트)
  누적 데이터, 그리고 "원인 후보로 지목한 뉴스가 실제로 맞았는지"를
  사후에(예: 1주일 뒤) 검증할 기준.
- **구현 난이도**: 중간. 피드백 수집 UI/채널(슬랙 버튼 등)과 저장소가
  필요하고, "원인 귀속 정확도"를 자동으로 채점하는 기준을 만드는 게
  특히 애매하고 손이 많이 간다 (결국 사람 판단이 섞여야 함).
- **에이전트 역할**: 에이전트 자체보다는 에이전트를 감독하는 별도
  파이프라인. 누적된 피드백을 분석해서 "이 유형의 이상치일 때는 이
  검색어 패턴이 유용했다" 같은 패턴을 뽑아 프롬프트/툴 설명 개선에
  반영 (수동 개선 프로세스에 데이터 근거를 제공하는 역할, 자동 프롬프트
  수정까지는 리스크가 커서 권장하지 않음).

### 10. 사내 데이터 연동 시 보안·권한 설계

- **운용역에게 주는 가치**: 포트폴리오 연동(#5) 등 사내 데이터를 쓰기
  시작하면 가치가 커지는 동시에 유출 리스크도 생긴다. 권한 설계가
  먼저 돼 있어야 사내 데이터 연동을 안전하게 확장할 수 있다.
  간접적이지만 이게 없으면 #5, #8 전부 막힌다.
- **필요한 데이터**: 없음 - 정책/설계 작업. 다만 사내 정보보안팀·컴플라
  이언스팀과의 협의가 선행 조건.
- **구현 난이도**: 기술적으로는 낮음(API 키 스코프 제한, 읽기 전용 계정,
  로그 감사 정도는 표준 패턴)이지만, 조직적 승인 절차 때문에 실제
  소요 시간은 길다.
- **에이전트 역할**: 에이전트가 호출할 수 있는 사내 데이터 툴은 반드시
  "읽기 전용"이어야 하고, 어떤 사내 데이터를 어느 범위까지 조회했는지
  지금의 `logs/*.jsonl`과 같은 방식으로 전부 감사 로그에 남겨야 한다
  (이 프로젝트가 이미 하고 있는 로깅 패턴을 사내 데이터로 확장하는 것).

---

## 인턴 기간 6개월 동안 현실적으로 해볼 수 있는 것

전부 다 하기엔 많다. 우선순위와 현실성 기준으로 추천하면:

> **2026-09-25 갱신**: #1(금투협 등급·섹터별 민평 스프레드)과 #3(국고채
> 입찰 결과 트래킹의 절반, 월별 발행 실적)은 이미 부분 구현됐다 - 각 항목
> 설명 참고. #2(국채선물 basis/외국인 수급)는 실제로 조사해보니 KRX
> 데이터 API가 로그인 필요로 막혀 있어서, 아래 "1~2개월차" 순서를 다시
> 짤 필요가 있다.

1. **1~2개월차**: **커브 RV 시그널(#7)**부터 - 추가 데이터 소스가
   필요 없어서(이미 확보한 국고채 커브 데이터로 충분) 데이터 접근성
   문제 없이 바로 손댈 수 있다. 그 다음 #1/#3에서 남은 과제(1년 lookback
   증분 백필, 등급평균 대비 자동 코멘트, 국고채 개별 입찰 낙찰금리/응찰률)를
   이어서 마무리.
2. **2~4개월차**: **국채선물 basis/외국인 수급(#2)**을 시도하되, 이제는
   무료 스크래핑이 아니라 유료 단말/KRX 정식 라이선스 확보부터
   알아보는 게 순서 - Phase E에서 무료 경로를 상당히 찾아봤지만 막혀
   있음을 확인했다 (`WORK_LOG.md` Phase 2-2/2-3, `ROADMAP.md` #2 참고).
3. **4~6개월차**: 인턴 기간 안에 조직적 승인이 걸리는 항목(포트폴리오
   연동 #5, 보안·권한 설계 #10)은 "설계 문서 + 프로토타입(가짜 포트폴리오
   데이터로)" 수준까지만 가는 게 현실적이다. 실제 사내 시스템 연동은
   승인 절차상 인턴 기간 안에 끝내기 어려울 가능성이 높다. 대신
   **리포트 품질 평가 체계(#9)**의 최소 버전(슬랙에 간단한 피드백
   버튼 붙이는 정도)은 6개월 안에 시도할 만하다.

멀티 에이전트(#8)와 term premium 자체 모델링(#6의 한국 버전)은 인턴
기간에는 설계 검토/작은 프로토타입 정도로 남기고, 본격 구현은 다음
사람에게 넘기는 게 합리적이다 - 둘 다 이 프로젝트의 나머지 부분이
안정화된 뒤에 손대는 게 리스크가 적다.
