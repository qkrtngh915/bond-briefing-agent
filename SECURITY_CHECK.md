# GitHub 푸시 전 보안 점검

이 저장소를 GitHub에 올리기 전에 실행한 점검이다. **실제 API 호출은 하지
않았다** - 전부 git 히스토리/로컬 파일에 대한 문자열 검색(grep)뿐이다.

## 점검 방법

`.env`에 있는 4개 키의 실제 값으로 다음을 검사했다:

1. **전체 git 히스토리(모든 커밋)의 파일 내용** - `git rev-list --all`로
   모든 커밋을 순회하며 각 커밋 시점의 트리에 `git grep -F`로 키 값이
   있는지 확인 (파일 내용 전체를 보는 방식이라, 나중에 지워졌어도 과거
   커밋에 남아있으면 잡힌다).
2. **모든 커밋의 diff(패치)** - `git log --all -S<키>` (pickaxe 검색)로,
   그 키 문자열이 추가되거나 삭제된 커밋이 있는지 교차 확인 (방법 1과
   결과가 같아야 정상).
3. **`.env` 파일 자체가 커밋된 적 있는지** - `git log --all --oneline -- .env`.
4. **현재 작업 디렉토리의 `logs/`, `data/cache/`, `data/extracted/`,
   `reports/`, `docs/`** - 키 값 자체뿐 아니라, API 요청 URL에 키가
   파라미터로 남아있을 가능성(`crtfc_key=`, `api_key=`, 각 API
   도메인 문자열)까지 검사.
5. **git으로 추적되는 전체 파일**(`git grep`, 워킹트리 기준) - 코드/문서
   어디에도 실제 키 값이 하드코딩되어 있지 않은지.
6. **`.gitignore`에 `.env`가 있는지, `.env`가 실제로 추적(tracked)되지
   않는지** 재확인.

검사한 키 4개: `ECOS_API_KEY`, `FRED_API_KEY`, `DART_API_KEY`,
`ANTHROPIC_API_KEY` (전부 `.env`에 있는 실제 값 그대로 검사했고, 이
문서에는 값 자체를 적지 않았다).

## 결과: 전부 깨끗함

| 점검 | 결과 |
|---|---|
| git 히스토리(전체 커밋 트리)에 키 값 | 없음 |
| git 커밋 diff(pickaxe)에 키 값 | 없음 |
| `.env` 파일이 커밋된 적 | 없음 (한 번도 추적된 적 없음) |
| `logs/`, `data/cache/`, `data/extracted/`, `reports/`, `docs/`에 키 값 | 없음 |
| 위 폴더에 요청 URL의 키 파라미터(`crtfc_key=`, `api_key=` 등) | 없음 |
| 추적 중인 파일(코드/문서) 전체에 키 값 하드코딩 | 없음 |
| `.gitignore`에 `.env` 등록 여부 | 등록되어 있음 |
| `.env`의 tracked 여부 | 추적되지 않음(정상) |

**결론: 지금 상태로 GitHub에 올려도 이 저장소를 통해 API 키가 노출될
경로는 없다.**

이렇게 깨끗한 이유를 코드 구조 관점에서 덧붙이면: `bond_agent/tools/`의
각 API 클라이언트(`ecos.py`, `fred.py`, `dart.py`)는 요청을 만들 때만
키를 URL/파라미터에 넣고, **반환값(캐시에 저장되고 로그에 남는 값)에는
키나 요청 URL을 아예 포함시키지 않는다** - 그래서 캐시/로그 파일 자체에
키가 새어 들어갈 구조적인 경로가 원래 없었다. 이번 점검은 "구조상
그럴 것"이라는 가정을 실제로 검증한 것이다.

## 만약 발견됐다면 (참고용 - 이번엔 필요 없었음)

지시에 따라 문제가 없었으므로 아무 조치도 실행하지 않았다. 그래도
참고를 위해, 만약 git 히스토리에서 키가 발견됐다면 제안했을 조치를
적어둔다 (**실행하지 않음**):

1. **즉시 그 키를 발급처에서 재발급/폐기.** 히스토리에서 지우는 것보다
   먼저 해야 한다 - 한 번 push됐던 키는 이미 노출된 것으로 간주해야
   한다 (fork/clone 등으로 이미 퍼졌을 수 있음).
2. **히스토리에서 제거**: 이 저장소는 아직 원격에 push된 적이 없으므로
   (전부 로컬 커밋), 비교적 안전하게 `git filter-repo`
   (권장, `git filter-branch`보다 빠르고 안전) 또는 BFG Repo-Cleaner로
   해당 파일/문자열을 전체 히스토리에서 제거하는 방법을 썼을 것이다.
   이미 원격에 push된 뒤였다면 force-push와 협업자 전체의 재클론이
   필요해서 훨씬 번거로웠을 것.
3. **재발 방지**: `.gitignore` 점검, pre-commit hook으로 커밋 전 키
   패턴 자동 검사(예: `detect-secrets`, `gitleaks`) 추가.
